# Copy to .env and fill in values before running docker-compose.prod.yml

# Database
POSTGRES_PASSWORD=change_me_strong_password

# API
SECRET_KEY=change_me_64_char_random_string

# External APIs (optional but needed for full verification)
UK_COMPANIES_HOUSE_API_KEY=
ANTHROPIC_API_KEY=
